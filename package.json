{
  "name": "studiopro-netlify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx netlify dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
